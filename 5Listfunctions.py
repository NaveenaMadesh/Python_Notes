# Insert
'''List.append(item) --> add at the end, accepts one value,return None
List.extend(iterable) --> Any iterable (list,tuple,string,set...) whose elements are added to the list
returns --> none --> modifies the list-in-place

List.insert(index,item)
index-->The position where the item should be inserted(integer)
item --> The elemeent to insert (can be any data type)
'''
#Removing
# l
'''ist.remove(value) ,The element to remove from the list
retuen -> none --> modifies the list in place

list.pop([index]), index is optional,if not provided removes the last and returns
'''

#Replace --> direct assignmnet
#index --> object.index(value,start,end)
'''value - The element to search for (required)
start (optional) - Starting index for the search (default: 0)
end (optional) - Ending index for the search (default: end of list)'''
#Raises value error if the value is not found in the list
#count--> object.count(value)

#copy()--> objects.copy()-->shallow copy

#clear--> clears the values from the list but the structure remains same


# keyword --> del
#